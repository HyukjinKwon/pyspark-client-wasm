{
  "name": "pyspark-connect-web-bridge-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Node unit tests for the main-thread SAB bridge (pyspark_connect_web/worker/bridge.js).",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
